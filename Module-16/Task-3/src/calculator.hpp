#pragma once
#include <string>

/**
 * @brief Calculate the mathematical expressions of addition, subtraction, multiplication, and division.
 * @param expression The mathematical expression.
 * @param result     Result of calculating a mathematical expression.
 * @return True if the calculation is successful, otherwise false.
 */
bool calculator(const std::string expression, double result[]);
